class NewsSource {
  String iconUrl;
  String websiteUrl;
  String name;
  String title;
  String feedUrl;
  bool isObserved;

  NewsSource({this.iconUrl, this.websiteUrl, this.name, this.title, this.isObserved, this.feedUrl});
}